# SPDX-FileCopyrightText: 2026 Thiago S. R. Silva, Diego S. Porto
# SPDX-License-Identifier: MIT

version: "3.9"

services:
  checker:
    build: .
    container_name: checker
    volumes:
      # Mount input data folder
      - ./data:/app/data
      # Mount outputs folder so results persist on host
      - ./outputs:/app/outputs
      # Mount configuration file
      - ./configs/config.yaml:/app/config.yaml
    environment:
      - ENV=production
    command: ["python", "checker/main.py"]